# 返回值状态码定义

**本项目程序自包依赖库及各函数一般返回格式如下：**
```json
return_msg = {
    "status": "success",
    "data": data,
    "code": 200
}
```
**我们取其中的`code`键值作为返回状态码，以便其他函数、程序判断运行状态**

## 状态码定义表
**我们为不同的状态码规定了具体含义（见下表）：**

| **`Code`值** | **定义** | **备注** |
| --- | --- | --- |
| **`200`** | **任务执行成功** | **-** |